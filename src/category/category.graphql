type Category {
  id: ID!
  name: String
  products: [Product]
}

input CreateCategoryInput {
  name: String!
}

type Query {
  category: [Category]!
}

type Mutation {
  createCategory(input: CreateCategoryInput!): Category!
}
